<Window x:Class="OpenAmp.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenAmp Studio" Width="448" Height="560"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Background="{StaticResource AccentBrush}" />
            <Grid Grid.Column="1" Margin="48,38,48,34">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
                <StackPanel Margin="0,0,0,32">
                    <Image Width="156" Height="48" Stretch="Uniform" HorizontalAlignment="Left">
                        <Image.Source>
                            <CroppedBitmap Source="/OpenAmp.Desktop;component/Assets/openamp-wordmark.png"
                                           SourceRect="126,420,788,180" />
                        </Image.Source>
                    </Image>
                    <TextBlock Text="STUDIO ADMINISTRACIJA" Style="{StaticResource EyebrowText}" Margin="2,9,0,0" />
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,0,0,18">
                    <TextBlock Text="Prijava" Style="{StaticResource PageTitle}" Margin="0,0,0,22" />
                <TextBlock Text="Username ili email" Style="{StaticResource MutedText}" Margin="0,0,0,7" />
                <TextBox Text="{Binding Identifier, UpdateSourceTrigger=PropertyChanged}" Height="42" />
            </StackPanel>
            <StackPanel Grid.Row="2" Margin="0,0,0,22">
                <TextBlock Text="Lozinka" Style="{StaticResource MutedText}" Margin="0,0,0,7" />
                <PasswordBox x:Name="PasswordInput" Height="42" KeyDown="PasswordInput_KeyDown" />
            </StackPanel>
            <Button Grid.Row="3" Content="Prijavi se" Style="{StaticResource AccentButton}"
                    Height="44" Click="Login_Click" />
            <TextBlock Grid.Row="4" Text="{Binding Error}" Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" Margin="0,14,0,0" />
                <TextBlock Grid.Row="5" VerticalAlignment="Bottom" Style="{StaticResource MutedText}"
                           FontSize="11" Text="Razvojni pristup: admin / OpenAmp1!" />
            </Grid>
        </Grid>
    </Border>
</Window>
